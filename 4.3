#include <stdio.h>

int main() {
    int a, b, c;
    
    // ขอให้ผู้ใช้กรอกหมายเลข 3 ค่า
    printf("Please enter three numbers: ");
    scanf("%d %d %d", &a, &b, &c);

    // คำนวณผลรวม
    int sum = a + b + c;

    // แสดงผลลัพธ์
    printf("Answer = %d\n", sum);

    return 0;
}
